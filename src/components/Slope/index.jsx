import React from 'react';

import styles from './slope.module.css';

const Slope = () => {
  return (
    <div className={styles.slopeContainer}>
      <div className={styles.slope} />
    </div>
  );
};

export default Slope;
